import type { FC } from 'react';

export const TestModeIndicator: FC = () => (
	<div className='body-tiny-bold fixed bottom-0 right-0 z-[1000] rounded-tl-[8px] bg-primary px-1 py-0.5 text-text2'>
		FOR TESTING
	</div>
);
